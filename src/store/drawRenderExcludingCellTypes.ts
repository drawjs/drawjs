import { ROTATE_ICON, SIZE_POINT, MINI_MAP } from "store/constant_cellTypeList"


/**
 * Cells not rendered in `draw.render()`
 * ( They may be rendered in other ways )
 */
export default [
	ROTATE_ICON,
	SIZE_POINT,
	MINI_MAP,
]
